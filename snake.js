define(
        'snake',
        [],
        function(){

                function snake(p) {

                        while (p.x ) {

                        }
                        
                         while( x < M && y < N && A[x+1] == B[y+1] ) {
                                             x += 1;
                                                         y += 1;
                                                                 }
                                                                         return y;
                }

                return snake;
        }
);
